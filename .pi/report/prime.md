# Prime CodepOS - Codebase Understanding Report

## Codebase Overview

**Project Name:** CodepOS  
**Location:** `/home/zerwiz/CodeP/CodepOS`  
**Status:** Initial project setup / Bootstrap repository

## Repository Structure

```
CodepOS/
├── CLAUDE.md           # Project configuration and guidelines
├── .pi/                 # PI (Personal Intelligence) system
│   ├── prompts/
│   │   ├── prime.md          # Prime command workflow
│   │   └── prime_cc.md       # Claude Code specific prime
│   └── agents/               # Subagent definitions (currently empty)
└── utils/
    └── settings.json         # Project settings
```

## Technology Stack

From `CLAUDE.md`:

| Category | Technology | Version |
|----------|------------|---------|
| Frontend | Bun | - |
| Backend | Python | 3.12 |
| Validation | Pydantic | 2.10.6 |
| Package Mgmt | Poetry | 1.9.0 |
| Testing | pytest | 8.3.4 |
| Linting | ruff | 0.9.2 |

## Development Guidelines

1. **Write tests first (TDD)** - Test-driven development approach
2. **TypeScript strict mode** - For frontend development
3. **Naming conventions** - Follow existing patterns
4. **JSDoc for public APIs** - Documentation requirements
5. **Function size limit** - Keep functions under 50 lines
6. **Zod schemas** - Input validation
7. **Result<T, E> pattern** - Error handling
8. **Repository pattern** - Database queries
9. **Standardized API responses** - Consistent API format

## Key Commands

| Command | Purpose |
|---------|---------|
| `bun run test` | Run tests before committing |
| `bun run lint` | Fix code style issues |
| `bun run build` | Production build with type checking |
| `uv run pytest` | Run tests with uv |

## Intended Project Structure

According to CLAUDE.md:
- `apps/frontend/` - Frontend source code (placeholder)
- `apps/backend/` - Backend source code (placeholder)
- `ai_docs/` - Additional documentation (placeholder)

## Current State

This repository appears to be in **initial setup** phase:
- No actual source code present yet
- Git repository initialized but no commits
- Only configuration files exist
- Ready for development to begin

## Development Workflow

1. Initialize project with `bun init` or `poetry init`
2. Create first tests using TDD approach
3. Set up frontend with TypeScript strict mode
4. Implement backend with concrete Pydantic models
5. Follow Result<T, E> pattern for error handling
6. Validate inputs with Zod schemas
7. Keep functions concise (< 50 lines)
8. Document public APIs with JSDoc

## Notes

- Project prioritizes **type safety** and **error handling**
- Uses **bun** over npm/yarn for package management
- Uses **uv** over pip/poetry for Python package management
- Emphasizes **test-first development** methodology

---

Report generated by Prime command execution.
